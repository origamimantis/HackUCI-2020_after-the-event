"use strict";

module.exports = class WrongNumberArgumentsError extends RangeError
{
    constructor(msg = "Wrong number of arguments in the called function.")
    {
        super(msg);
        this.name = this.constructor.name;
    }
};