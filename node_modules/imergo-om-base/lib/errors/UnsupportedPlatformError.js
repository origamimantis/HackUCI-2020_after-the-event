"use strict";

module.exports = class UnsupportedPlatformError extends Error
{
    constructor(msg = "The platform used to run the application is not supported.")
    {
        super(msg);
        this.name = this.constructor.name;
    }
};