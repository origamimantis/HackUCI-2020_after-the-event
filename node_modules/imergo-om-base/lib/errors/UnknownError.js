"use strict";

module.exports = class UnknownError extends Error
{
    constructor(msg = "Unknown error. Please check the stack trace.")
    {
        super(msg);
        this.name = this.constructor.name;
    }
};