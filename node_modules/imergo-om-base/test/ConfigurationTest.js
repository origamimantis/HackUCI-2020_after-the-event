"use strict";

describe("imergo-om-base#Configuration", () =>
{
    const assert = require("assert");
    const iob = require("..");

    it("should init Configuration", () =>
    {
        const configuration = new iob.Configuration();
        assert(configuration instanceof iob.Configuration);
    });
});
